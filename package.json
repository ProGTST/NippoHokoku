{
  "name": "nippo-desktop",
  "version": "0.1.0",
  "description": "日報入力デスクトップアプリ（案A: WebView組み込み / Electron）",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "pack": "electron-packager . NippoHokoku --platform=win32 --arch=x64 --out=../NippoHokoku-pack --overwrite --tmpdir=false --ignore=\"^/dist\" --ignore=\"^/\\.git\"",
    "dist": "electron-builder --win portable",
    "dist:nsis": "electron-builder --win nsis"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "prettier": "^3.9.4",
    "typescript": "^5.9.3"
  },
  "build": {
    "appId": "jp.co.genech.nippo.desktop",
    "productName": "日報入力",
    "files": [
      "main.js",
      "preload.js",
      "src/**/*"
    ],
    "win": {
      "target": [
        "portable"
      ]
    },
    "portable": {
      "artifactName": "日報入力-${version}.exe"
    },
    "directories": {
      "output": "dist"
    }
  }
}
